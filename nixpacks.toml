[phases.setup]
aptPkgs = ["python3", "python3-pip", "python3-venv", "ffmpeg"]

[phases.install]
cmds = [
  "pip3 install --no-cache-dir -r requirements.txt"
]

[build]
builder = "NIXPACKS"
buildCommand = "npm install"

[deploy]
startCommand = "node index.js"